#include <PCM.h>

//#include "duemilanove.h"
//#include "shotgun.h"
#include "dog.h"

void setup()
{
  //startPlayback(sample, sizeof(sample));
  //startPlayback(shotgun_data, shotgun_length);
  startPlayback(dog_data, dog_length);
}

void loop()
{
}
